# RFC-0000: <Proposal Title>

> Copy this file to `docs/rfc/NNNN-<short-slug>.md` (zero-padded, next
> available number) and fill it in. RFCs are for **exploration and
> input gathering**; ADRs are for **commitments**. An RFC ends in one
> of: (a) promoted to ADR, (b) rejected, (c) deferred.
>
> See `AGENTS.md` §15.

| | |
| --- | --- |
| Status | `draft` / `discussion` / `accepted` / `rejected` / `deferred` |
| Date | YYYY-MM-DD |
| Author | @handle |
| Reviewers | @handle1, @handle2 (need ≥2 +1 to promote to ADR) |
| Plan section affected | Sec. NN |
| Related RFCs / ADRs | … |

## Problem Statement

What user / engineering / business problem are we trying to solve?
Quantify if possible (latency, cost, conversion %, customer count).

## Goals

- Goal 1: …
- Goal 2: …

## Non-goals

- Things explicitly out of scope for this proposal.

## Background & Constraints

Relevant context the reader needs (existing architecture, prior
decisions, regulatory constraints, vendor commitments, team size,
timeline pressure).

Link to:

- Plan section.
- Existing ADRs.
- Prior RFCs (accepted, rejected, deferred).

## Proposal

The recommended approach. Include:

- Architecture sketch (ASCII diagram or link to image).
- Data model changes.
- API surface changes.
- Migration plan.
- Telemetry / observability.
- Rollout plan (feature flag, phased rollout, kill-switch).

## Alternatives

For each alternative considered:

- Summary.
- Pros / cons.
- Why not.

(If none, say so explicitly: "No alternatives were viable because …".)

## Risks & Open Questions

| Risk / Question | Severity | Mitigation / Owner |
| --- | --- | --- |
| … | low/med/high | … |

If there are open questions that the RFC cannot resolve alone, list
them here so reviewers can engage.

## Cost Estimate

- Engineering days.
- Infra cost delta (per month, year 1, year 3).
- Vendor cost delta.
- Maintenance burden (ongoing).

## Success Metrics

How will we know this worked 30 / 90 days after rollout?

- Metric 1 (with target).
- Metric 2 (with target).

## Decision Log (filled during review)

| Date | Reviewer | Position | Comment |
| --- | --- | --- | --- |
| YYYY-MM-DD | @handle | +1 / -1 / abstain | "..." |

## Outcome

Final disposition (`accepted` → ADR-NNNN, `rejected`, or `deferred`)
and a 1–2 sentence summary of why.

## References

- Plan section.
- External docs / blog posts / vendor pages.
- Related issues / Linear tickets.
